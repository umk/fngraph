import { Context } from '@fngraph/data'

/**
 * A record at the output of generator that aggregates result
 * of querying multiple data sources.
 */
type AggregateRecord = Context

export default AggregateRecord
