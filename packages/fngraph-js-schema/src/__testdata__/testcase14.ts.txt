/**
 * Returns a generator that yields incremented values.
 *
 * @param start - The starting value of the counter.
 * @param end - The end value (exclusive) of the counter.
 * @returns A generator that produces a sequence of incremented values.
 *
 * @remarks
 * This function is useful for creating a synchronous sequence of values.
 */
export function* counterGenerator(start: number, end: number): Generator<number> {
}